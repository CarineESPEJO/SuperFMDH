<form method="get" action="{{ actionPath }}" class="catalog-search">
    <input type="text" name="q" value="{{ query|default('') }}" placeholder="Rechercher...">
    <button type="submit">Rechercher</button>
</form>