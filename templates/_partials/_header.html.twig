<header>
    <nav>
        <div>
            <a href="{{ path('home')}}">
                <img src=" {{asset('images/logo_big.PNG')}}" alt="Logo">
                <b>Find My Dream Home</b>
            </a>
        </div>


        <ul>
            <li><a href="{{ path('houses')}}">Maisons</a></li>
            <li><a href="{{ path('apartments')}}">Appartements</a></li>
            <li><a href="{{ path('allListings')}}">Annonces</a></li>

            {% if app.user %}
            <li><a href="{{ path('favorites')}}">Favoris</a></li>

            {% if 'ROLE_ADMIN' in app.user.roles or 'ROLE_AGENT' in app.user.roles %}
            <li class="login"><a href="{{ path('createlisting')}}">Ajouter</a></li>
            {% endif %}

            <li class="login"><a href="{{ path('app_logout')}}">Déconnexion</a></li>
            {% else %}
            <li class="login"><a href="{{ path('app_login')}}">Connexion</a></li>
            {% endif %}

        </ul>
    </nav>
</header>